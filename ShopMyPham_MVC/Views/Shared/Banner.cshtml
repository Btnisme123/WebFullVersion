@model List<ShopMyPham_MVC.Framework.Banner>
<section class="slider">
    <div class="flexslider">
        <ul class="slides">
            @foreach (var item in Model)
            {
                <li><img src="@item.Image" alt="" /></li>
            }
           
           
        </ul>
    </div>
</section>
